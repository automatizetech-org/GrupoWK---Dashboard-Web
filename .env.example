# Exemplo de variáveis de ambiente (NÃO coloque segredos aqui)
#
# Copie para ".env" e preencha com seus valores locais.
# Em produção (Vercel), configure em Settings -> Environment Variables.

# Proteção por senha no dashboard (produção)
DASHBOARD_ACCESS_PASSWORD=

# Supabase (valores do seu projeto)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role (NUNCA commitar a chave real)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

